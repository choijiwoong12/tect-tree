"use client";

import type { TreeNode } from "@/types/tree";

export function UnlockModal({ node: _node, onClose: _onClose }: { node: TreeNode; onClose: () => void }) {
  return <div>{/* TODO */}</div>;
}
